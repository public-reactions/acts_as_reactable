/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/

# rspec failure tracking
.rspec_status

.DS_Store

# otherwise the version in lockfile will mismatch the updated one during the release flow
/Gemfile.lock
